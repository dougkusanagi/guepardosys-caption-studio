# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environment
.venv/

# Build artifacts
build/
dist/

# OS / editor
.DS_Store
.vscode/
.idea/

# Local outputs
*.wav
*.mp4
*.mkv

# Web editor cache
web/uploads/
web/processed/
web/projects/
!web/uploads/.gitkeep
!web/processed/.gitkeep
!web/projects/.gitkeep

# Tauri
src-tauri/target/
src-tauri/gen/

# Sidecar build artifacts
sidecar/dist/
sidecar/build/
sidecar/*.spec

# Bundled binaries (downloaded / built externally)
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep

# Whisper models (downloaded at runtime or bundled during build)
models/
